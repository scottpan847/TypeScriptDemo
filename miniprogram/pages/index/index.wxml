<!--{{page}}.wxml-->
<<<<<<< HEAD
<map 
  class='map' 
  id='map' 
  longitude="{{longitude}}" 
  latitude="{{latitude}}" 
  scale="{{scale}}" 
  show-location="true" 
  markers="{{markers}}" 
  enable-3D="{{is3D}}"
  enable-overlooking="{{isoverlooking}}"
  bindmarkertap="bindMakertap" 
  bindcontroltap='controlTap' 
  bindregionchange='regionChange' 
  controls='{{controls}}'
  bindtap='bindMapTap'></map>
=======

<map 
  id="map"
  latitude="{{location.latitude}}"
  longitude="{{location.longitude}}"
  scale="{{scale}}"
  setting="{{setting}}"
  enable-overlooking="{{isOverLooking}}"
  enable-3D="{{is3D}}"
  markers="{{markers}}"
>
<cover-view class="icon-container">
    <cover-image src="../../resources/account.png"></cover-image>
    <cover-image 
      src="../../resources/location.png"
      bindtap="onMyLocationTap"></cover-image>
  </cover-view >
  <cover-view class="btn-container bg-grey round">
    <cover-view>扫码注册</cover-view>
  </cover-view>
</map>
>>>>>>> main
